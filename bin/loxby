#!/usr/bin/env ruby
# frozen_string_literal: true

# require_relative '../lib/loxby/runner'
require 'loxby/runner'

# Start interpreter when run as script
Lox::Runner.new.run(ARGV) if __FILE__ == $PROGRAM_NAME
